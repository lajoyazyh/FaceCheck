spring:
  config:
    activate:
      on-profile: prod
  jpa:
    properties:
      hibernate:
        format_sql: false

server:
  forward-headers-strategy: framework

logging:
  level:
    root: INFO
    com.facecheck: INFO
